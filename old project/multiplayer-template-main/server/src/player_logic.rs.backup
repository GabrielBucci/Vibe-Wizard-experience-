use spacetimedb::*;
use crate::common::*;

// Player movement constants
const PLAYER_SPEED: f32 = 5.0;
const SPRINT_MULTIPLIER: f32 = 1.8;

pub fn update_player_position(input: &InputState, current_position: &Position, current_rotation: &Rotation) -> Position {
    // ... player movement logic ...
    Position { x: 0.0, y: 0.0, z: 0.0 }
}

// ... rest of player logic ... 